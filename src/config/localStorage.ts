const PREFIX_NAME = '@gameplay';

const collection_users = `${PREFIX_NAME}:user`;
const collection_appointments = `${PREFIX_NAME}:appointments`;

export const localStorageConfig = {
  collection_users,
  collection_appointments
};